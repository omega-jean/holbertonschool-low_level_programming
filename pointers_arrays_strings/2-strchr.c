#include "main.h"

/**
  * _strspn - function that locates a character in a string.
  * @s: string
  * @c: character
  *
  * Return: 
  */

char *_strchr(char *s, char c)
{
int i;
for (i = 0; s != '\n', i++)
if (s == 

return ('\0')
}
